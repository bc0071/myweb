pipeline {
    agent any
    stages {
      stage('Git Checkout') {
            steps {
                echo 'No needed to checkout'
                echo 'Remove/comment git'
                // git 'https://github.com/bc0071/myweb'
            }
        }
    }
}
